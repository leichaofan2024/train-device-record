<h3><%= @station.name %></h3>


<table class="table table-bordered">
  <thead>
    <tr>
      <th>名称</th>
    </tr>
  </thead>
  <tbody>
    <% @equipment_categories.each do |equipment_category| %>
    <tr>
      <td><%= link_to(equipment_category.name)%></td>
    </tr>
    <% end %>
  </tbody>
</table>
