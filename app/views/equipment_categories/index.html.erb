<div class="category-frame">
  <% @equipment_categories.each_with_index do |e,index|%>
      <% p= new_equipment_category_transformer_path(e) %>
      <div class="category-item">
        <%= link_to p,class: "btn btn-default" do %>
          <%= e.name%>
        <% end %>
      </div>

  <% end %>
  <div class="add-category">
    <%= link_to("添加设备分类", edit_station_path(current_station),class: "btn btn-default")%>
  </div>

</div>
